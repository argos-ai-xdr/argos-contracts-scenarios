# schema/

Cubierto hoy por `scripts/validate.sh` (cada `schemas/**/v*.schema.json` es Draft 2020-12 válido y declara `additionalProperties` explícitamente) y por `../../validators/validate_fixtures.py` (cada fixture valida/falla según la carpeta que le corresponde). No hay pruebas adicionales aquí todavía — este README existe para que la carpeta se mantenga en el árbol y documente dónde vive realmente la lógica.
