# Mapping ECS

El documento maestro v0.5 (6.1) fija OCSF como alineación primaria del envelope, con "mapping ECS en Wazuh/OpenSearch" para la ruta específica de ingesta Wazuh (ADR-001). Este directorio documenta esa correspondencia ECS (Elastic Common Schema) para los campos que Wazuh/OpenSearch consumen directamente, sin pasar por el envelope OCSF-aligned genérico.

Pendiente (ARG-015): tabla de mapping real una vez esté desplegado `platform/wazuh/` en `argos-platform`.
