# manifests/

Manifiesto agregado: qué snapshot de `../cti/` y `../vulnerability-db/` se usó en cada `release-manifest.yaml` de `argos-control/releases/`. Vacío hasta la primera release que incluya datos reales (posterior a `0.1.0-dev`, que aún no tiene servicios desplegados).
